export interface IPopularProduct {
    id: number;
    title: string;
    imageURL: string;
    description: string;
    price: number;
}

export interface IProduct {
    id: number;
    title: string;
    imageURL: string;
    description: string;
    price: number;
}